#!/usr/bin/env python3\n\"\"\"\nТкрипт для анализа сохраненных данных о производительности торговых стратегий.\nПозволяет просматривать статистику, экспортировать отчеты и анализировать эффективность.\n\"\"\"\n\nimport json\nimport sys\nfrom pathlib import Path\nfrom datetime import datetime\nfrom typing import Dict, List\n\ndef load_strategy_data(data_file: str = \"strategy_performance.json\") -> Dict:\n    \"\"\"Загружает данные о производительности стратегий из файла\"\"\"\n    data_path = Path(data_file)\n    \n    if not data_path.exists():\n        print(f\"❌ Файл {data_file} не найден!\")\n        print(\"Убедитесь, что бот уже запускался и сохранил данные.\")\n        return {}\n    \n    try:\n        with open(data_path, 'r', encoding='utf-8') as f:\n            data = json.load(f)\n            return data.get('strategy_performance', {})\n    except Exception as e:\n        print(f\"❌ Ошибка при загрузке файла: {e}\")\n        return {}\n\ndef print_summary_report(strategy_data: Dict):\n    \"\"\"Выводит краткий отчет по всем стратегиям\"\"\"\n    print(\"\\n\" + \"=\"*60)\n    print(\"📊 СВОДНЫЙ ОТЧЕТ ПО СТРАТЕГИЯМ\")\n    print(\"=\"*60)\n    \n    if not strategy_data:\n        print(\"📭 Нет данных для отображения\")\n        return\n    \n    # Сортируем стратегии по общему PnL\n    sorted_strategies = sorted(\n        strategy_data.items(),\n        key=lambda x: x[1].get('total_pnl', 0),\n        reverse=True\n    )\n    \n    print(f\"{'Стратегия':<15} | {'Сделки':<8} | {'Win%':<6} | {'PnL':<12} | {'Avg PnL':<8} | {'Sharpe':<6}\")\n    print(\"-\" * 70)\n    \n    total_trades = 0\n    total_wins = 0\n    total_pnl = 0.0\n    \n    for name, perf in sorted_strategies:\n        trades = perf.get('total_trades', 0)\n        wins = perf.get('winning_trades', 0)\n        pnl = perf.get('total_pnl', 0.0)\n        sharpe = perf.get('sharpe_ratio', 0.0)\n        \n        if trades > 0:\n            win_rate = (wins / trades) * 100\n            avg_pnl = pnl / trades\n            print(f\"{name:<15} | {wins:>2}/{trades:<4} | {win_rate:>5.1f}% | ${pnl:>10.2f} | ${avg_pnl:>6.2f} | {sharpe:>5.2f}\")\n            \n            total_trades += trades\n            total_wins += wins\n            total_pnl += pnl\n        else:\n            print(f\"{name:<15} | {'0/0':<7} | {'N/A':<6} | {'$0.00':<12} | {'N/A':<8} | {'N/A':<6}\")\n    \n    print(\"-\" * 70)\n    if total_trades > 0:\n        overall_win_rate = (total_wins / total_trades) * 100\n        overall_avg_pnl = total_pnl / total_trades\n        print(f\"{'ИТОГО':<15} | {total_wins:>2}/{total_trades:<4} | {overall_win_rate:>5.1f}% | ${total_pnl:>10.2f} | ${overall_avg_pnl:>6.2f} | {'—':<6}\")\n\ndef print_detailed_report(strategy_data: Dict, strategy_name: str = None):\n    \"\"\"Выводит детальный отчет по конкретной стратегии или всем стратегиям\"\"\"\n    if strategy_name:\n        if strategy_name not in strategy_data:\n            print(f\"❌ Стратегия '{strategy_name}' не найдена!\")\n            print(f\"Доступные стратегии: {', '.join(strategy_data.keys())}\")\n            return\n        strategies_to_show = {strategy_name: strategy_data[strategy_name]}\n    else:\n        strategies_to_show = strategy_data\n    \n    for name, perf in strategies_to_show.items():\n        print(f\"\\n🎯 ДЕТАЛЬНЫЙ ОТЧЕТ: {name.upper()}\")\n        print(\"=\"*50)\n        \n        trades = perf.get('total_trades', 0)\n        wins = perf.get('winning_trades', 0)\n        losses = perf.get('losing_trades', 0)\n        pnl = perf.get('total_pnl', 0.0)\n        avg_duration = perf.get('avg_trade_duration', 0.0)\n        max_drawdown = perf.get('max_drawdown', 0.0)\n        sharpe = perf.get('sharpe_ratio', 0.0)\n        vol_pref = perf.get('volatility_preference', 1.0)\n        trend_pref = perf.get('trend_preference', 1.0)\n        \n        print(f\"📈 Общая статистика:\")\n        print(f\"   • Всего сделок: {trades}\")\n        print(f\"   • Успешных: {wins} ({(wins/trades*100) if trades > 0 else 0:.1f}%)\")\n        print(f\"   • Убыточных: {losses} ({(losses/trades*100) if trades > 0 else 0:.1f}%)\")\n        print(f\"   • Общий PnL: ${pnl:.2f}\")\n        print(f\"   • Средний PnL за сделку: ${(pnl/trades) if trades > 0 else 0:.2f}\")\n        print(f\"   • Средняя длительность: {avg_duration:.1f} мин\")\n        print(f\"   • Максимальная просадка: {max_drawdown:.2f}%\")\n        print(f\"   • Sharpe ratio: {sharpe:.3f}\")\n        \n        print(f\"\\n⚙️ Настройки стратегии:\")\n        print(f\"   • Предпочтение волатильности: {vol_pref:.1f}\")\n        print(f\"   • Предпочтение тренда: {trend_pref:.1f}\")\n        \n        # Последние сделки\n        recent_trades = perf.get('last_10_trades', [])\n        if recent_trades:\n            print(f\"\\n📋 Последние {min(len(recent_trades), 5)} сделок:\")\n            for i, trade in enumerate(recent_trades[-5:], 1):\n                success = \"✅\" if trade.get('success', False) else \"❌\"\n                pnl_trade = trade.get('pnl', 0)\n                duration = trade.get('duration', 0)\n                timestamp = trade.get('timestamp', '')\n                if isinstance(timestamp, str):\n                    try:\n                        timestamp = datetime.fromisoformat(timestamp).strftime('%Y-%m-%d %H:%M')\n                    except:\n                        timestamp = timestamp[:16] if len(timestamp) > 16 else timestamp\n                print(f\"   {i}. {success} ${pnl_trade:+.2f} ({duration}мин) - {timestamp}\")\n\ndef export_to_csv(strategy_data: Dict, output_file: str = \"strategy_report.csv\"):\n    \"\"\"Экспортирует данные в CSV файл для анализа в Excel\"\"\"\n    try:\n        import csv\n        \n        with open(output_file, 'w', newline='', encoding='utf-8') as csvfile:\n            fieldnames = [\n                'strategy', 'total_trades', 'winning_trades', 'losing_trades',\n                'win_rate', 'total_pnl', 'avg_pnl_per_trade', 'avg_duration',\n                'max_drawdown', 'sharpe_ratio', 'volatility_preference', 'trend_preference'\n            ]\n            writer = csv.DictWriter(csvfile, fieldnames=fieldnames)\n            \n            writer.writeheader()\n            \n            for name, perf in strategy_data.items():\n                trades = perf.get('total_trades', 0)\n                wins = perf.get('winning_trades', 0)\n                \n                row = {\n                    'strategy': name,\n                    'total_trades': trades,\n                    'winning_trades': wins,\n                    'losing_trades': perf.get('losing_trades', 0),\n                    'win_rate': (wins / trades * 100) if trades > 0 else 0,\n                    'total_pnl': perf.get('total_pnl', 0.0),\n                    'avg_pnl_per_trade': (perf.get('total_pnl', 0) / trades) if trades > 0 else 0,\n                    'avg_duration': perf.get('avg_trade_duration', 0.0),\n                    'max_drawdown': perf.get('max_drawdown', 0.0),\n                    'sharpe_ratio': perf.get('sharpe_ratio', 0.0),\n                    'volatility_preference': perf.get('volatility_preference', 1.0),\n                    'trend_preference': perf.get('trend_preference', 1.0)\n                }\n                writer.writerow(row)\n        \n        print(f\"✅ Данные экспортированы в {output_file}\")\n    except Exception as e:\n        print(f\"❌ Ошибка при экспорте: {e}\")\n\ndef main():\n    \"\"\"Главная функция скрипта\"\"\"\n    if len(sys.argv) < 2:\n        print(\"📊 Анализатор производительности торговых стратегий\")\n        print(\"\\nИспользование:\")\n        print(f\"  {sys.argv[0]} summary                    - Краткий отчет по всем стратегиям\")\n        print(f\"  {sys.argv[0]} details [strategy_name]   - Детальный отчет\")\n        print(f\"  {sys.argv[0]} export [output.csv]       - Экспорт в CSV\")\n        print(f\"  {sys.argv[0]} best                      - Лучшие стратегии\")\n        print(\"\\nПримеры:\")\n        print(f\"  {sys.argv[0]} summary\")\n        print(f\"  {sys.argv[0]} details sma_crossover\")\n        print(f\"  {sys.argv[0]} export my_report.csv\")\n        return\n    \n    command = sys.argv[1].lower()\n    data_file = \"strategy_performance.json\"\n    \n    # Проверяем, есть ли файл данных\n    strategy_data = load_strategy_data(data_file)\n    if not strategy_data:\n        return\n    \n    if command == \"summary\":\n        print_summary_report(strategy_data)\n        \n    elif command == \"details\":\n        strategy_name = sys.argv[2] if len(sys.argv) > 2 else None\n        print_detailed_report(strategy_data, strategy_name)\n        \n    elif command == \"export\":\n        output_file = sys.argv[2] if len(sys.argv) > 2 else \"strategy_report.csv\"\n        export_to_csv(strategy_data, output_file)\n        \n    elif command == \"best\":\n        print(\"\\n🏆 ЛУЧШИЕ СТРАТЕГИИ ПО КАТЕГОРИЯМ\")\n        print(\"=\"*40)\n        \n        # Лучшая по PnL\n        best_pnl = max(strategy_data.items(), key=lambda x: x[1].get('total_pnl', 0))\n        print(f\"💰 Лучшая по прибыли: {best_pnl[0]} (${best_pnl[1].get('total_pnl', 0):.2f})\")\n        \n        # Лучшая по win rate\n        strategies_with_trades = {k: v for k, v in strategy_data.items() if v.get('total_trades', 0) > 0}\n        if strategies_with_trades:\n            best_winrate = max(strategies_with_trades.items(), \n                             key=lambda x: x[1]['winning_trades'] / x[1]['total_trades'])\n            wr = (best_winrate[1]['winning_trades'] / best_winrate[1]['total_trades']) * 100\n            print(f\"🎯 Лучшая по винрейту: {best_winrate[0]} ({wr:.1f}%)\")\n            \n            # Лучшая по Sharpe\n            best_sharpe = max(strategies_with_trades.items(), \n                            key=lambda x: x[1].get('sharpe_ratio', 0))\n            print(f\"📈 Лучшая по Sharpe: {best_sharpe[0]} ({best_sharpe[1].get('sharpe_ratio', 0):.3f})\")\n        \n        print_summary_report(strategy_data)\n        \n    else:\n        print(f\"❌ Неизвестная команда: {command}\")\n        print(\"Доступные команды: summary, details, export, best\")\n\nif __name__ == \"__main__\":\n    main()\n